ja:
  pages:
    admin: 管理
    top: ホーム

  actions:
    close: 閉じる
    sign_in: ログイン
    sign_out: ログアウト

  messages:
    require_javascript: |
      JavaScriptが無効のため、正常に動作させることができません。
      このWebシステムへのアクセスにはJavaScriptが必要です。JavaScriptを有効にしてください。
    require_modern_browser: |
      レガシブラウザーを使用しているため、正常に動作させることができません。
      このWebシステムへのアクセスには最新のモダンブラウザーが必要です。
      Internet Explorer等のレガシブラウザーは使用できません。
      Microsoft Edge、Mozilla Firefox、Google Chrome、Apple Safari等のモダンブラウザーを使用してください。

  date:
    formats:
      default: "%Y年%m月%d日(%a)"
      long: "%Y/%m/%d"
      short: "%m/%d"

  time:
    formats:
      default: "%Y年%m月%d日(%a) %H時%M分%S秒 %z"
      long: "%Y/%m/%d %H:%M:%S"
      short: "%m/%d %H:%M"

  activerecord:
    models:
      confirmation: 確認
      hardware: ハードウェア
      ip6_pool: IPv6プール
      ip_pool: IPプール
      networck: ネットワーク
      nic: ネットワークインタフェースコントローラー(NIC)
      node: 端末
      operating_system: オペレーティングシステム(OS)
      place: 設置場所
      security_software: セキュリティソフトウェア
      # network_category: ネットワークカテゴリー
      # network_connection: ネットワーク接続
      # network_interface: ネットワークインターフェース
      # subnetwork: サブネットワーク
      # subnetwork_user: ユーザーのサブネットワーク
      # ip_address: IPアドレス
      # ip_network: IPネットワーク
    attributes:
      hardware:
        device_type: 機器形態
        maker: 製造業者
        product_name: 製品名
        model_number: 型番
      network:
        name: 名前
        vlan: VLAN番号
        dhcp: DHCP
        auth: 認証
        closed: 閉域
        ip_address: IPアドレス
        ip_mask: サブネットマスク
        ip_gateway: ゲートウェイ
        ip6_address: IPv6アドレス
        ip6_prefix: プレフィックス
        ip6_gateway: IPv6ゲートウェイ
        note: 備考
      nic:
        node: 端末
        name: 名前
        interface_type: 接続形態
        mac_address: MACアドレス
        duid: DUID(DHCP固有識別子)
        network: ネットワーク
        ip_config: IP設定
        ip_address: IPアドレス
        ip6_config: IPv6設定
        ip6_address: IPv6アドレス
      node:
        name: 端末名
        hostname: ホスト名
        domain: ドメイン名
        location: 設置場所
        hardware: 機器情報
        operating_system: OS
        security_software: セキュリティソフトウェア
        network_interface: ネットワークインターフェース
        note: 備考
      operating_system:
        os_category: OS種別
        name: OS名
        eol: サポート終了日
        description: 詳細
      place:
        area: 拠点(キャンパス)
        building: 建屋
        floor: フロア
        room: 部屋
      security_software:
        name: ソフトウェア名
        description: 詳細
      user:
        username: ユーザー名
        password: パスワード
      # ip_address:
      #   network_connection: ネットワーク接続
      #   address: IPアドレス
      # subnetwork:
      #   name: 名前
      #   network_category: ネットワークカテゴリー
      #   vlan: VLAN番号
      # network_category:
      #   name: 名前
      #   dhcp: DHCP
      #   auth: 認証
      #   global: グローバル
      # network_connection:
      #   network_interface: ネットワークインターフェース
      #   subnetwork: サブネットワーク
      #   mac_address_randomization: MACアドレスのランダム化
      # network_interface:
      #   name: インターフェース名
      #   interface_type: 接続形態
      #   mac_address: MACアドレス(ハードウェアアドレス)
      #   duid: DUID(DHPv6固有識別子)
    enums:
      device_types:
        desktop: デスクトップPC
        laptop: ノートPC (2-in-1を含む)
        tablet: タブレット
        mobile: 携帯端末 (スマートフォン/携帯電話)
        server: サーバー
        appliance: 専用機器 (アプライアンスサーバー)
        network: ネットワーク機器 (ルーター/スイッチ等)
        virtual: 仮想マシン
        other: その他
        unknown: 不明
      os_categories:
        windows_client: Windows クライアント
        windows_server: Windows サーバー
        mac: Mac (macOS/Mac OS X等)
        linux: Linux (Ubuntu/CentOS等)
        bsd: BSD系UNIX (FreeBSD/OpenBSD等)
        unix: System V系UNIX (Solaris/AIX等)
        apple: Apple端末OS (iOS/iPadOS/watchOS/tvOS等)
        android: Android
        dedicated: 専用OS
        embedded: 組込みOS
        os_less: OS無し
        other: その他
        unknwon: 不明
      interface_types:
        wired: 有線
        wireless: 無線
        virtual: 仮想
        bluetooth: Bluetooth
        dialup: 電話回線
        vpn: VPN
        other: その他
      ip_configs:
        disabled: 無効
        static: 固定(手動設定)
        dynamic: 動的(DHCP)
        reserved: 予約(DHCP)
        link_local: リンクローカル(自動設定)
      ip6_configs:
        disabled: 無効
        static: 固定(手動設定)
        dynamic: 動的(自動取得)
        reserved: 予約(自動取得)
        link_local: リンクローカル(自動設定)


