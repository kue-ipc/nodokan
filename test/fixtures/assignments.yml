# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# adminには何も関連付けない

assignment:
  user: user
  network: network
  auth: true
  use: true
  default: true
  manage: true

staff_server:
  user: staff
  network: server
  auth: false
  use: true
  default: false
  manage: false

staff_client:
  user: staff
  network: client
  auth: true
  use: true
  default: false
  manage: false

staff_ipv4only:
  user: staff
  network: ipv4only
  auth: false
  use: true
  default: false
  manage: false

staff_ipv6only:
  user: staff
  network: ipv6only
  auth: false
  use: true
  default: false
  manage: false

staff_noip:
  user: staff
  network: noip
  auth: false
  use: true
  default: false
  manage: false

staff_last:
  user: staff
  network: last
  auth: false
  use: true
  default: true
  manage: false

guest_client:
  user: guest
  network: client
  auth: true
  use: false
  default: false
  manage: false

# mangeが可能
other_client:
  user: other
  network: client
  auth: true
  use: true
  default: false
  manage: true

