# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# adminには何も関連付けない

user_server:
  user: user
  network: server
  auth: false
  use: true
  manage: false

user_client:
  user: user
  network: client
  auth: true
  use: true
  manage: false

user_ipv4only:
  user: user
  network: v4only
  auth: false
  use: true
  manage: false

user_ipv6only:
  user: user
  network: v6only
  auth: false
  use: true
  manage: false

user_noip:
  user: user
  network: noip
  auth: false
  use: true
  manage: false

guest_client:
  user: guest
  network: client
  auth: true
  use: false
  manage: false

# mangeが可能
other_client:
  user: other
  network: client
  auth: true
  use: true
  manage: true

