{
  "extends": "stylelint-config-standard",
  "rules": {
    "selector-class-pattern": null
  },
  "overrides": [
    {
      "files": [
        "*.scss",
        "**/*.scss"
      ],
      "extends": "stylelint-config-standard-scss"
    }
  ]
}
