<%== "\u{feff}" -%>
<%- require 'csv' -%>
<%= %w[
  action
  id
  username
  email
  fullname
  role
  deleted
  auth_network
  networks
  result
  message
].join(',') %>
<%- @users.each do |user| -%>
<%==
[
  '',
  user.id,
  user.username,
  user.email,
  user.fullname,
  user.role,
  user.deleted,
  user.auth_network&.identifier,
  user.use_networks.map(&:identifier).join(' '),
  'success',
  '',
].to_csv
-%>
<%- end -%>
